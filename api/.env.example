# api — environment variables (ALL OPTIONAL).
# Every key below has a working default — copy to .env only to OVERRIDE one
# locally; the app runs fine with no .env at all. `.env` is gitignored — never
# commit real values.
# In production these come from Kubernetes (ConfigMap for non-secret keys,
# Secret for sensitive ones).
#
# Format (godotenv):
#   - plain KEY=value, one per line
#   - no `export ` prefix; quotes optional (godotenv strips them)
#   - values already set in the environment win over this file
#
# Every key has a working default (shown); REDIS_PASSWORD is the only sensitive
# one (blank locally).

# --- HTTP server ---
HTTP_ADDR=:8090                 # 8080 is taken by kafka-ui
GO_ENV=dev
REQUEST_TIMEOUT=5s              # per-request deadline (Go duration units)
CORS_ORIGINS=*                  # comma-separated origins; * = allow any

# --- Redis ---
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=                 # sensitive (blank = no auth)
REDIS_DB=0
REDIS_KEY_PREFIX=flight:
