apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Long-lived stateful services (applied via APPLY_INFRA=1, not on app deploys).
resources:
  - namespace.yaml
  - kafka.yaml
  - kafka-init.yaml
  - redis.yaml
  - postgres.yaml
  - monitoring
